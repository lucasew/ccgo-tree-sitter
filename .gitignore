/parse
__pycache__
.codex

# Grammar C sources under third-party/tree-sitter-*/ are vendored (committed).
# Refresh with: mise run grammars:sync (workspaced core:place).
#
# Full application trees — NOT grammars. Optional local corpora only; never vendor.
/third-party/tensorflow/
/third-party/django/
.workspaced/

# Never vendor compiler objects under placed trees
*.o
*.a
*.so
# Experimental leaven raw output
leaven-out/
